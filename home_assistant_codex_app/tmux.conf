# Load these before the tmux server creates its first client. Applying terminal
# overrides after the server has started is too late for the initial xterm
# attachment and leaves the browser in an alternate screen without scrollback.
set -g mouse on
set -g alternate-screen off
set -as terminal-overrides ',xterm*:smcup@:rmcup@'
